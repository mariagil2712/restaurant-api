provider "aws" {       # Declara proveedor AWS para gestionar recursos con Terraform (fuente: Terraform Provider Configuration)
  region = "us-east-1" # Región objetivo donde se crearán recursos (fuente: AWS regional services model)
}